// Firebase stub — the web JS SDK (`firebase` package) is not compatible with React Native
// without native modules. All Firebase functionality is replaced by AsyncStorage.
// To re-enable Firebase, set up @react-native-firebase native modules and update this file.

export const firebaseApp = null as any;
export const auth = null as any;
export const db = null as any;

export function getFirebaseAuth() {
  return null;
}

export default null;
